<h3>Register here</h3>

<% if @user.errors.any? %>
  <div class="error-box">
    <h3><%= pluralize(@user.errors.count, "error") %> prohibited your account from being created:</h3>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div><br>
<% end %>

<%= render "form" %>

<br> <br>

<%= link_to 'Back', root_path %>